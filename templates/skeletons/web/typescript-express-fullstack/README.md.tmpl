# {{ .Project.Name }}

This project was generated with Greicodex CLI.

## Prerequisites

- Node.js 18
- Docker
- npm

## Getting Started

1.  **Install dependencies:**

    ```bash
    npm install
    ```

2.  **Run the application:**

    ```bash
    npm start
    ```

3.  **Run with Docker Compose:**

    ```bash
    docker-compose up -d
    ```

## Deployment

### Docker

To deploy with Docker, you can use the provided `Dockerfile.tmpl` and `docker-compose.yml.tmpl` as a starting point.
