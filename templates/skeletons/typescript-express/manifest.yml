name: typescript-express
description: Pila para crear una API en Typescript con Express.
type: api
provides:
  language: Typescript
  tooling: Express
options:
  persistence:
    message: "¿Qué tipo de persistencia usarás?"
    values:
      - "None"
      - "PostgreSQL"
      - "MySQL"
  deployment:
    message: "¿Dónde quieres desplegar la aplicación?"
    values:
      - "Kubernetes"
      - "Lambda"
  linter:
    message: "¿Qué linter quieres usar?"
    values:
      - "ESLint"
      - "TSLint"
  packageManager:
    message: "¿Qué gestor de paquetes quieres usar?"
    values:
      - "npm"
      - "yarn"
  cicd:
    message: "¿Qué proveedor de CI/CD quieres usar?"
    values:
      - "GitHub Actions"
      - "GitLab CI"
