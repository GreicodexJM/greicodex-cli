# {{ .Project.Name }}

This project was generated with Greicodex CLI.

## Prerequisites

- PHP 8.2
- Docker
- Composer

## Getting Started

1.  **Install dependencies:**

    ```bash
    composer install
    ```

2.  **Run the application with Docker Compose:**

    ```bash
    docker-compose up -d
    ```

## Deployment

{{ if eq .Stack.deployment "Lambda" }}
### AWS Lambda

To deploy to AWS Lambda, you'll need to have the [Serverless Framework](https://www.serverless.com/) and [Bref](https://bref.sh/) installed and your AWS credentials configured.

Then, run the following command:

```bash
serverless deploy
```

{{ else if eq .Stack.deployment "KNative" }}
### KNative

To deploy to KNative, you'll need to have `kn` CLI installed and your Kubernetes cluster configured.

Then, run the following command:

```bash
kn service apply -f service.yaml
```
{{ end }}
