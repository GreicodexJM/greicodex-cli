{
  "name": "{{ .Project.Name }}",
  "version": "1.0.0",
  "description": "Project description of {{ .Project.Name }}",
  "main": "dist/main.js",
  "scripts": {
    "start": "node dist/main.js",
    "build": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only src/main.ts",
    "lint": "{{ if eq .Stack.linter "ESLint" }}eslint .{{ else }}tslint --project .{{ end }}"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.11.7",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.4.4"{{ if eq .Stack.linter "ESLint" }},
    "eslint": "^8.2.0"{{ else }},
    "tslint": "^6.1.3"{{ end }}
  }
}
